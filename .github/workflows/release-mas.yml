# DEPRECATED: Tauri MAS builds have been replaced by Electron.
# MAS support for the Electron app will be added in a future workflow.

name: Release Mac App Store (Tauri) [DEPRECATED]

on:
  workflow_dispatch:
    inputs:
      _deprecated:
        description: 'This workflow is deprecated. MAS builds are pending for Electron.'
        required: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::warning::This workflow is deprecated. MAS support for Electron is coming."
          exit 1
