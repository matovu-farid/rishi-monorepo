---
phase: 02-epub-reader
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/components/LibraryEmptyState.tsx
  - apps/mobile/components/BookRow.tsx
autonomous: true
requirements: [FILE-01, FILE-06]
must_haves:
  truths:
    - "Home tab shows a list of imported books with title and author"
    - "Empty state shows 'No books yet' message with 'Import Book' CTA button"
    - "Tapping 'Import Book' opens system file picker for EPUB files"
    - "After importing, the new book appears at the top of the list"
    - "Tapping a book row navigates to /reader/[id] route"
    - "FAB appears when library has books for additional imports"
  artifacts:
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Library screen replacing Phase 1 placeholder Home screen"
      min_lines: 50
    - path: "apps/mobile/components/LibraryEmptyState.tsx"
      provides: "Empty state component with icon, heading, body, CTA"
      exports: ["LibraryEmptyState"]
    - path: "apps/mobile/components/BookRow.tsx"
      provides: "Single book row in library list"
      exports: ["BookRow"]
  key_links:
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/lib/file-import.ts"
      via: "importEpubFile() call on button press"
      pattern: "importEpubFile"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/lib/book-storage.ts"
      via: "getBooks() call to load library"
      pattern: "getBooks"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/app/reader/[id].tsx"
      via: "router.push with book id"
      pattern: "router\\.push.*reader"
---

<objective>
Replace the Phase 1 placeholder Home screen with the Library screen showing imported books, and wire up the file import flow.

Purpose: Users need a way to see their books and import new ones. This is the entry point to the reading experience.
Output: Library screen with empty state, book list, import button, and navigation to reader.
</objective>

<execution_context>
@/Users/faridmatovu/.claude/get-shit-done/workflows/execute-plan.md
@/Users/faridmatovu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-epub-reader/02-UI-SPEC.md
@.planning/phases/02-epub-reader/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 (types and services) -->

From apps/mobile/types/book.ts:
```typescript
export interface Book {
  id: string
  title: string
  author: string
  coverPath: string | null
  filePath: string
  format: 'epub'
  currentCfi: string | null
  createdAt: number
}
```

From apps/mobile/lib/book-storage.ts:
```typescript
export function getBooks(): Book[]
export function getBookById(id: string): Book | null
```

From apps/mobile/lib/file-import.ts:
```typescript
export async function importEpubFile(): Promise<Book | null>
```

From apps/mobile/constants/theme.ts:
```typescript
export const Colors = {
  light: { text: '#11181C', background: '#fff', tint: '#0a7ea4', icon: '#687076' },
  dark: { text: '#ECEDEE', background: '#151718', tint: '#fff', icon: '#9BA1A6' },
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BookRow and LibraryEmptyState components</name>
  <files>apps/mobile/components/BookRow.tsx, apps/mobile/components/LibraryEmptyState.tsx</files>
  <read_first>
    - apps/mobile/components/themed-text.tsx (existing component pattern)
    - apps/mobile/types/book.ts (Book interface from Plan 01)
    - apps/mobile/components/ui/icon-symbol.tsx (icon component pattern)
    - apps/mobile/components/ui/icon-symbol.ios.tsx (iOS icon variant)
  </read_first>
  <action>
Create `apps/mobile/components/BookRow.tsx`:

A pressable row component displaying a single book in the library list.

```tsx
import { View, Text, Pressable } from 'react-native'
import { Book } from '@/types/book'

interface BookRowProps {
  book: Book
  onPress: (book: Book) => void
}

export function BookRow({ book, onPress }: BookRowProps) {
  return (
    <Pressable
      className="flex-row items-center px-4 py-4 active:bg-gray-100 dark:active:bg-gray-800"
      onPress={() => onPress(book)}
      accessibilityRole="button"
      accessibilityLabel={`Open ${book.title} by ${book.author}`}
    >
      {/* Cover placeholder -- 48px wide, gray background, rounded */}
      <View className="w-12 h-16 bg-gray-200 dark:bg-gray-700 rounded-md items-center justify-center mr-4">
        <Text className="text-gray-400 dark:text-gray-500 text-xs">EPUB</Text>
      </View>
      <View className="flex-1">
        <Text
          className="text-base font-semibold text-gray-900 dark:text-white"
          numberOfLines={1}
        >
          {book.title}
        </Text>
        <Text
          className="text-sm text-gray-500 dark:text-gray-400 mt-0.5"
          numberOfLines={1}
        >
          {book.author}
        </Text>
      </View>
    </Pressable>
  )
}
```

Create `apps/mobile/components/LibraryEmptyState.tsx`:

Empty state shown when no books are imported. Centered vertically with icon, heading, body text, and CTA.

```tsx
import { View, Text, TouchableOpacity, ActivityIndicator } from 'react-native'
import { IconSymbol } from '@/components/ui/icon-symbol'

interface LibraryEmptyStateProps {
  onImport: () => void
  importing: boolean
}

export function LibraryEmptyState({ onImport, importing }: LibraryEmptyStateProps) {
  return (
    <View className="flex-1 items-center justify-center px-6">
      <IconSymbol name="book.fill" size={48} color="#9BA1A6" />
      <Text className="text-xl font-semibold text-gray-900 dark:text-white mt-4">
        No books yet
      </Text>
      <Text className="text-base text-gray-500 dark:text-gray-400 text-center mt-2 mb-6">
        Import an EPUB from your device to start reading.
      </Text>
      <TouchableOpacity
        className="w-full bg-[#0a7ea4] rounded-lg py-3 items-center"
        onPress={onImport}
        disabled={importing}
        accessibilityRole="button"
        accessibilityLabel="Import Book"
      >
        {importing ? (
          <ActivityIndicator color="#FFFFFF" />
        ) : (
          <Text className="text-white font-semibold text-base">Import Book</Text>
        )}
      </TouchableOpacity>
    </View>
  )
}
```

UI spec compliance:
- Empty state heading: "No books yet"
- Empty state body: "Import an EPUB from your device to start reading."
- CTA text: "Import Book"
- Import button: accent background (#0a7ea4), white text, rounded-lg, 48px height via py-3
- Book row: 80px+ min height (py-4 = 16px padding top+bottom + content), cover 48px wide
- Touch targets: buttons and rows are >= 44px touchable area
- Accessibility labels on all interactive elements
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/rishi-monorepo/apps/mobile && node -e "var fs=require('fs'),br=fs.readFileSync('components/BookRow.tsx','utf8'),es=fs.readFileSync('components/LibraryEmptyState.tsx','utf8');if(br.indexOf('BookRow')===-1)throw 'no BookRow';if(br.indexOf('book.title')===-1)throw 'no title';if(br.indexOf('book.author')===-1)throw 'no author';if(br.indexOf('onPress')===-1)throw 'no onPress';if(es.indexOf('No books yet')===-1)throw 'no empty heading';if(es.indexOf('Import an EPUB')===-1)throw 'no empty body';if(es.indexOf('Import Book')===-1)throw 'no CTA';if(es.indexOf('0a7ea4')===-1)throw 'no accent color';console.log('Components OK')"</automated>
  </verify>
  <acceptance_criteria>
    - components/BookRow.tsx exports `BookRow` component accepting `book: Book` and `onPress` props
    - BookRow renders book.title in a Text with numberOfLines={1} and font-semibold
    - BookRow renders book.author in a Text with numberOfLines={1} and text-gray-500
    - BookRow has a 48px wide cover placeholder View with rounded-md
    - BookRow wraps in Pressable with accessibilityRole="button"
    - components/LibraryEmptyState.tsx exports `LibraryEmptyState` component
    - LibraryEmptyState contains text "No books yet"
    - LibraryEmptyState contains text "Import an EPUB from your device to start reading."
    - LibraryEmptyState contains TouchableOpacity with text "Import Book"
    - Import button uses bg-[#0a7ea4] for accent color
    - LibraryEmptyState accepts `importing` boolean prop and shows ActivityIndicator when true
  </acceptance_criteria>
  <done>BookRow and LibraryEmptyState components created matching UI spec copywriting and layout.</done>
</task>

<task type="auto">
  <name>Task 2: Replace Home screen with Library screen and wire import flow</name>
  <files>apps/mobile/app/(tabs)/index.tsx, apps/mobile/app/(tabs)/_layout.tsx</files>
  <read_first>
    - apps/mobile/app/(tabs)/index.tsx (current Phase 1 placeholder to replace)
    - apps/mobile/app/(tabs)/_layout.tsx (tab layout configuration)
    - apps/mobile/components/BookRow.tsx (created in Task 1)
    - apps/mobile/components/LibraryEmptyState.tsx (created in Task 1)
    - apps/mobile/lib/file-import.ts (importEpubFile from Plan 01)
    - apps/mobile/lib/book-storage.ts (getBooks from Plan 01)
  </read_first>
  <action>
Rewrite `apps/mobile/app/(tabs)/index.tsx` to be the Library screen:

```tsx
import { useCallback, useEffect, useState } from 'react'
import { View, FlatList, TouchableOpacity, Alert, Text } from 'react-native'
import { SafeAreaView } from 'react-native-safe-area-context'
import { useRouter } from 'expo-router'
import { IconSymbol } from '@/components/ui/icon-symbol'
import { BookRow } from '@/components/BookRow'
import { LibraryEmptyState } from '@/components/LibraryEmptyState'
import { getBooks } from '@/lib/book-storage'
import { importEpubFile } from '@/lib/file-import'
import { Book } from '@/types/book'

export default function LibraryScreen() {
  const router = useRouter()
  const [books, setBooks] = useState<Book[]>([])
  const [importing, setImporting] = useState(false)

  const loadBooks = useCallback(() => {
    const loaded = getBooks()
    setBooks(loaded)
  }, [])

  useEffect(() => {
    loadBooks()
  }, [loadBooks])

  const handleImport = useCallback(async () => {
    setImporting(true)
    try {
      const book = await importEpubFile()
      if (book) {
        loadBooks()
      }
    } catch (error) {
      Alert.alert(
        'Import Failed',
        'Could not import book. The file may be corrupted or not a valid EPUB.'
      )
      console.error('Import error:', error)
    } finally {
      setImporting(false)
    }
  }, [loadBooks])

  const handleBookPress = useCallback(
    (book: Book) => {
      router.push(`/reader/${book.id}`)
    },
    [router]
  )

  if (books.length === 0) {
    return (
      <SafeAreaView className="flex-1 bg-white dark:bg-[#151718]">
        <LibraryEmptyState onImport={handleImport} importing={importing} />
      </SafeAreaView>
    )
  }

  return (
    <SafeAreaView className="flex-1 bg-white dark:bg-[#151718]">
      <View className="px-6 pt-4 pb-2">
        <Text className="text-2xl font-semibold text-gray-900 dark:text-white">
          Library
        </Text>
      </View>
      <FlatList
        data={books}
        keyExtractor={(item) => item.id}
        renderItem={({ item }) => (
          <BookRow book={item} onPress={handleBookPress} />
        )}
        contentContainerClassName="pb-24"
      />
      {/* FAB for importing when library has books */}
      <TouchableOpacity
        className="absolute bottom-6 right-6 w-14 h-14 rounded-full bg-[#0a7ea4] items-center justify-center shadow-lg"
        onPress={handleImport}
        disabled={importing}
        accessibilityRole="button"
        accessibilityLabel="Import Book"
      >
        <IconSymbol name="plus" size={24} color="#FFFFFF" />
      </TouchableOpacity>
    </SafeAreaView>
  )
}
```

Update `apps/mobile/app/(tabs)/_layout.tsx`:

Change the Home tab configuration to reflect Library:
- Change tab title from "Home" to "Library"
- Change icon from "house.fill" to "book.fill"
- Keep everything else the same (auth guard, API init, Explore tab)

Specifically, update the `Tabs.Screen` for `name="index"`:
```tsx
<Tabs.Screen
  name="index"
  options={{
    title: 'Library',
    tabBarIcon: ({ color }) => <IconSymbol size={28} name="book.fill" color={color} />,
  }}
/>
```

Note: The `/reader/[id]` route does not exist yet -- it will be created in Plan 03. The navigation call `router.push('/reader/${book.id}')` will work once that route exists. For now, the Library screen is functional for importing and listing books.

UI spec compliance:
- Error message uses Alert.alert with copy: "Could not import book. The file may be corrupted or not a valid EPUB."
- FAB: bottom-right, 56px (w-14 h-14), circle, accent background, plus icon in white
- Library heading: 28px equivalent (text-2xl), font-semibold
- SafeAreaView from react-native-safe-area-context (NOT react-native)
- Dark mode background: #151718 (from Colors.dark.background)
  </action>
  <verify>
    <automated>cd /Users/faridmatovu/projects/rishi-monorepo/apps/mobile && node -e "var fs=require('fs'),idx=fs.readFileSync('app/(tabs)/index.tsx','utf8'),lay=fs.readFileSync('app/(tabs)/_layout.tsx','utf8');if(idx.indexOf('importEpubFile')===-1)throw 'no importEpubFile';if(idx.indexOf('getBooks')===-1)throw 'no getBooks';if(idx.indexOf('LibraryEmptyState')===-1)throw 'no empty state';if(idx.indexOf('BookRow')===-1)throw 'no BookRow';if(idx.indexOf('router.push')===-1)throw 'no navigation';if(idx.indexOf('reader/')===-1)throw 'no reader route';if(idx.indexOf('react-native-safe-area-context')===-1)throw 'wrong SafeAreaView';if(lay.indexOf('Library')===-1)throw 'no Library title';if(lay.indexOf('book.fill')===-1)throw 'no book icon';console.log('Library screen OK')"</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/index.tsx imports and calls `getBooks` from '@/lib/book-storage'
    - app/(tabs)/index.tsx imports and calls `importEpubFile` from '@/lib/file-import'
    - app/(tabs)/index.tsx renders `LibraryEmptyState` when books array is empty
    - app/(tabs)/index.tsx renders `BookRow` components via FlatList when books exist
    - app/(tabs)/index.tsx navigates to `/reader/${book.id}` on book press via router.push
    - app/(tabs)/index.tsx renders FAB with bg-[#0a7ea4] and plus icon when books exist
    - app/(tabs)/index.tsx imports SafeAreaView from 'react-native-safe-area-context'
    - app/(tabs)/index.tsx shows Alert on import error with text "Could not import book"
    - app/(tabs)/_layout.tsx has tab title 'Library' for the index screen
    - app/(tabs)/_layout.tsx uses 'book.fill' icon for the index tab
  </acceptance_criteria>
  <done>Library screen replaces Phase 1 placeholder. Books display in a list. Import flow works via FAB and empty state CTA. Book tap navigates to reader route.</done>
</task>

</tasks>

<verification>
- Library screen renders empty state when no books exist
- Import button opens file picker and creates a DB record
- Book list shows imported books with title and author
- FAB visible when library is populated
- Tab bar shows "Library" with book icon
- Navigation to /reader/[id] is wired (route created in Plan 03)
</verification>

<success_criteria>
Users can see the Library screen, import EPUB files from device storage, see them in the list, and tap to navigate toward the reader (reader route built in Plan 03).
</success_criteria>

<output>
After completion, create `.planning/phases/02-epub-reader/02-02-SUMMARY.md`
</output>
