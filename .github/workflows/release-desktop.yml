# DEPRECATED: Tauri desktop builds have been replaced by Electron.
# See release-electron.yml for the active release workflow.
#
# This file is kept to avoid breaking any external references to the workflow name.
# It will never trigger because the 'on' filter matches nothing.

name: Release Desktop (Tauri) [DEPRECATED]

on:
  workflow_dispatch:
    inputs:
      _deprecated:
        description: 'This workflow is deprecated. Use release-electron.yml instead.'
        required: false

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "::warning::This workflow is deprecated. The desktop app is now built with Electron."
          echo "See .github/workflows/release-electron.yml"
          exit 1
