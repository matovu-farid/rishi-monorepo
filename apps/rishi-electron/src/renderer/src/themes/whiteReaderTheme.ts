import { ReaderTheme } from './commonReader'

const whiteReaderTheme: ReaderTheme = {
  background: '#fff',
  color: '#000',
  backgroundArrowColor: '#fff',
  arrowColor: '#000'
}

export default whiteReaderTheme
