// Intentionally empty.
//
// Xcode's SwiftPM integration expects CNumKong to emit a linkable object file.
// The package is header-only by design, so this stub gives the build system a
// real C source to compile without changing runtime behavior.
