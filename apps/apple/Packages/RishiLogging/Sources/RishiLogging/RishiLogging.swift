// RishiLogging — os.Logger wrappers + Sentry breadcrumb adapter.
// Phase 1 adds the actual Logger(subsystem:category:) wrappers and Sentry init gate.
public enum RishiLogging { public static let placeholder = "RishiLogging" }
